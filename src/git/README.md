# Attribution

[`asyncgit`](https://github.com/gitui-org/gitui) MIT

Copied and modified from their `sync` module, generating diffs, status, commit,
and staging.

Ideally, I didn't want to introduce an async runtime as well as the dependencies
needed from using the crate.
